.DS_Store
.workbuddy-ai/
.workbuddy/
dist/
deploy/.env
*.tar.gz

__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 本地测试产物
backend/.pytest-tmp/
frontend/test-results/
frontend/playwright-report/

# 前端验收截图与本地记录（约 15MB，只在本机留存）；
# 交付用的正式证据在 deploy/evidence/ 下，那部分才进库。
frontend/docs/

# 依赖与虚拟环境（本地生成，不进库）
node_modules/
.venv/
venv/

# ── 凭据与环境变量：一律不进库 ────────────────────────────────
# 只放"值在哪里"，不放值本身；示例文件（.env.example）例外。
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
**/credentials*.json
**/*creds*.env
# 验收凭据按流程只经环境变量传递（见 frontend/deployment-tests/），
# 若有人临时落盘成脚本或 shell 片段，也不许进库。
**/dd-ops-*.env
**/*-creds.sh
**/id_rsa*
**/id_ed25519*

# ── 第三方材料与外部快照：不是本仓库的产物，不作为仓库内容 ──────
# 材料/ 含对方提供的对接文档、网页归档（约 45MB）与原始数据；
# 参考代码/dingdong 是外部参考仓库快照。两者都是本地输入，
# 清单与结论写在 材料清单.md / 参考代码/来源说明.md 里。
材料/
参考代码/dingdong/

# ── 本机交接产物：只在机器之间搬运，一律不进库 ──────────────────
# 这些文件含公网入口 IP、SSH 别名与私钥文件名、内网/Tailscale 地址，
# 只在本机与交接包之间流通（见 文档/文档索引.md「版本库里有什么」）。
# 若有人把它们复制进仓库，规则在此兜住；正式入库的交付文档不含这些信息。
*-接续提示词.md
*-交接包-*.tar.gz
HANDOVER.md
**/ssh-config*
**/*-hosts.env
**/*-deploy-host*

# ── 本机测试环境备忘：账号/端口/启动命令，只在本机流通 ──────────
本地测试环境.md

# ── 自循环驱动的运行产物：每轮的原始输出与临时状态，只在本机 ──────
# 每轮 grok 的 JSON/stderr 会很大且含会话细节，不入库；轮次摘要走
# .trellis/loop/runs.log（一行一轮，入库）。STOP 是停机信号，*.done 是
# 轮次完成标记，都是本机运行态，同样不入库。
.trellis/loop/runs/
.trellis/loop/STOP
.trellis/loop/*.done

# ── 本机 AI 工具自动配置：IDE 装 skills 时生成的本地产物 ──────────
# .trae/ 是 Trae IDE 的 skills 安装目录（如 typesafe-ai/Jev 调用指引），
# skills-lock.json 是对应的版本锁文件；均为本机工具自动生成与更新，
# 不同机器内容不同，不入库。
.trae/
skills-lock.json


