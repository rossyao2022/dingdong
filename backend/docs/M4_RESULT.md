# M4 家长前端迁移与真实 API 联调

> 历史阶段验收记录：保留当时测试数量和结果，不代表当前总量。最新见 [M5验收](M5_RESULT.md) 与 [启动后界面测试](../../frontend/docs/UI_FUNCTIONAL_20260912.md)。

参考仓库 HEAD 与 origin/main 均为 `d754a5bf9ea8e71ca64a850d2e26aa321fe8ab38`。迁移到独立 frontend，保留兴趣岛和伙伴视觉；活动、档案、问卷、授权、关联、报告、服务回执读取实际后端，移除原演示业务存储和手选指纹评分路径。

本轮增加 GET `/api/v1/children/{child_id}/assessments`，分页返回完整会话，以支持跨浏览器恢复；答案与版本字段复用 AssessmentSession 契约。按家庭鉴权，跨家庭返回 404。OpenAPI 已同步，共 49 个操作。没有数据库表结构变更。

登录联调修复验证码请求与提交间的竞态；重载问卷可从服务端恢复到合成输入确认页。撤回同步授权后明确展示授权状态；最后一个儿童删除后仍能查看家长回执。

## 验证

- 后端：83 项 PostgreSQL 测试通过，覆盖率 92%。原始输出 `tdd-green-m4.txt`。
- 前端：5 项真实浏览器端到端测试通过。先写浏览器用例，初始空页面运行失败（`../../frontend/docs/tdd-red-m4.txt`），实现后运行真实浏览器验收，最终输出见 `../../frontend/docs/tdd-green-m4.txt`。
- 浏览器覆盖：短信/JWT 登录，档案，活动完整流程和刷新恢复；22 题、合成输入、异步初始报告；机器人凭据核验、实际同步/阶段报告、撤回用途；390px 移动布局、儿童切换、跨页退出；家长删除申请、后台实际处理、无儿童时回执。
- 浏览器不使用 route fulfill 或业务响应 mock。合成输入写入数据库；关联、任务、画像与报告通过实际服务产生。
- Python 检查、迁移漂移检查和前端语法检查随本轮验收执行。截图：`../../frontend/docs/desktop-login.png`、`../../frontend/docs/mobile-explore.png`。

## 当前边界

本地前端 http://127.0.0.1:4173 ，后端 http://127.0.0.1:8017 。真实短信仍未接入，验证码字符串 00000；DingDong API 字段、核验协议、观察窗口和专业算法仍待确认。真实指纹没有上传入口，后端只接受确定性非生物合成图片。当前结果不是专业测评结论。本轮没有公开部署。
