...........F                                                             [100%]
=================================== FAILURES ===================================
_________ test_exploration_completion_records_choices_without_profile __________

client = <rest_framework.test.APIClient object at 0x10a1db230>

    def test_exploration_completion_records_choices_without_profile(client):
        import uuid
        from test_assessments import setup_assessment
        from dingdong_ca.core.models import ProfileSnapshot
        child, grant, old, _, _ = setup_assessment(client, answer=False)
        config = client.get('/api/v1/assessment-config', {'purpose': 'exploration'}).json()
>       assert config['purpose'] == 'exploration'
               ^^^^^^^^^^^^^^^^^
E       KeyError: 'purpose'

tests/test_questionnaires.py:61: KeyError
=========================== short test summary info ============================
FAILED tests/test_questionnaires.py::test_exploration_completion_records_choices_without_profile
1 failed, 11 passed in 1.98s
