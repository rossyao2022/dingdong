/* Original eight-dimension questionnaire: answer-derived scores, no synthetic defaults. */
(() => {
 'use strict';
 const {questions,dimensions,options,version}=window.TalentData;
 const KEY='dingdong-talents-v1', valid=v=>Number.isInteger(v)&&v>=1&&v<=5;
 const blank=()=>({version,index:0,answers:{},completed:false,completedAt:null});
 let state=blank(),bridge={},storageOK=true;
 const complete=()=>questions.every(q=>valid(state.answers[q.id]));
 try{const s=JSON.parse(localStorage.getItem(KEY));if(s?.version===version){for(const q of questions)if(valid(s.answers?.[q.id]))state.answers[q.id]=s.answers[q.id];state.index=Number.isInteger(s.index)?Math.max(0,Math.min(23,s.index)):0;state.completed=!!s.completed&&complete();state.completedAt=state.completed&&typeof s.completedAt==='string'?s.completedAt:null;}}catch{storageOK=false;}
 function save(){try{localStorage.setItem(KEY,JSON.stringify(state));storageOK=true;}catch{storageOK=false;}}
 function scores(){return dimensions.map(d=>{const answers=questions.filter(q=>q.type===d.id).map(q=>state.answers[q.id]);return {...d,score:answers.every(valid)?answers.reduce((a,b)=>a+b,0):null};});}
 const link='<a class="text-button" href="#explore">← 回到天赋探索</a>';
 function summary(){return `<a class="talent-summary" href="#talents"><span class="talent-summary-icon">✦</span><div><small>8 DIMENSIONS · 24 QUESTIONS</small><h3>八大天赋优势</h3><p>${state.completed?'已完成 · 查看八维画像、逐项解释和行动建议':Object.keys(state.answers).length?`已回答 ${Object.keys(state.answers).length} / 24 题，接着发现自己`:'语言、音乐、逻辑、空间、身体、内省、人际、自然'}</p></div><b>${state.completed?'查看报告':'开启探索'} →</b></a>`;}
 function render(){if(state.completed)return report();return `${link}<section class="talent-welcome"><div><span class="wonder-eyebrow">EIGHT WAYS TO SHINE</span><h1>你有八种，<em>闪闪发光的可能。</em></h1><p>从熟悉的小事里，发现自己的优势线索。<br>和家长一起，按最近一段时间的真实表现作答。</p><div class="talent-welcome-actions"><button class="button" data-talent-action="start">${Object.keys(state.answers).length?'继续上次的探索':'和 DingDong 一起开始'} →</button><span>24 道题 · 约 5–8 分钟</span></div></div><div class="talent-orbit"><img src="assets/dingdong/robot-wave.webp" alt="DingDong 陪你发现八种可能">${dimensions.map((d,i)=>`<span style="--i:${i};--dim:${d.color}" title="${d.name}">${d.icon}</span>`).join('')}</div></section><div class="talent-intro-grid">${dimensions.map((d,i)=>`<article style="--dim:${d.color}"><span>${d.icon}</span><small>0${i+1} / ${d.name}</small><h3>${d.nickname}</h3><p>${d.desc}</p></article>`).join('')}</div><div class="talent-method"><b>怎样回答更有帮助？</b><p>每个维度 3 题，选项从“完全不符合”到“完全符合”，每题 1–5 分。家长可以结合具体事例与孩子商量；暂时拿不准可选“一般”，之后补充观察再修改。可以中途退出并继续。</p><p>沿用原版 24 题与八维计分。这是一份日常表现观察问卷，结果不代表标准化能力分数，也不为孩子固定类型。${storageOK?'答案只保存在当前浏览器。':'当前无法使用浏览器存储，刷新后答案可能丢失。'}</p></div>`;}
 function question(){const q=questions[state.index],d=dimensions.find(d=>d.id===q.type);bridge.dialog?.('每一个小发现，都算数。',`<div class="dialog-body talent-question" style="--dim:${d.color}"><div class="talent-question-top"><span>${d.icon} ${d.name}</span><b>第 ${state.index+1} / 24 题</b></div><div class="talent-progress" role="progressbar" aria-label="八维问卷答题进度" aria-valuemin="0" aria-valuemax="24" aria-valuenow="${Object.keys(state.answers).length}"><i style="width:${Object.keys(state.answers).length/24*100}%"></i></div><p class="talent-prompt-hint">想一想，最近的自己符合这句话吗？</p><h3 id="talent-question-title" tabindex="-1">${q.text}</h3><div class="talent-options" role="group" aria-label="符合程度">${options.map((label,i)=>`<button data-talent-action="answer" data-value="${i+1}" class="${state.answers[q.id]===i+1?'selected':''}" aria-pressed="${state.answers[q.id]===i+1}"><span>${i+1}</span>${label}<b aria-hidden="true">${state.answers[q.id]===i+1?'✓':'○'}</b></button>`).join('')}</div><p class="small-print">没有标准答案，选择真实的自己就好。${storageOK?'可以退出，进度会自动保存。':'当前仅在页面内保留，刷新后可能丢失。'}</p></div>`,`<button class="button ghost" data-talent-action="previous" ${state.index===0?'disabled':''}>上一题</button><button class="button" data-talent-action="next" ${valid(state.answers[q.id])?'':'disabled'}>${state.index===23?'生成我的八维报告':'下一题 →'}</button>`,'DINGDONG · EIGHT WAYS TO SHINE');}
 function radar(rows){const order=['word','logic','space','music','body','social','self','nature'];const ds=order.map(id=>rows.find(d=>d.id===id));const point=(i,r)=>[200+Math.sin(i*Math.PI/4)*r,190-Math.cos(i*Math.PI/4)*r];return `<svg class="talent-radar" viewBox="0 0 400 390" role="img" aria-label="八维表现雷达图，各维度原始分见下方详细卡片"><title>八维观察得分，满分 15</title>${[1,2,3,4,5].map(n=>`<polygon points="${ds.map((_,i)=>point(i,n*25).join(',')).join(' ')}" fill="${n%2?'#f8f3ff':'#fff'}" stroke="#e6deee"/>`).reverse().join('')}${ds.map((_,i)=>`<line x1="200" y1="190" x2="${point(i,125)[0]}" y2="${point(i,125)[1]}" stroke="#e6deee"/>`).join('')}<polygon points="${ds.map((d,i)=>point(i,d.score/15*125).join(',')).join(' ')}" fill="#aa7ccb44" stroke="#9560b5" stroke-width="3"/>${ds.map((d,i)=>`<circle cx="${point(i,d.score/15*125)[0]}" cy="${point(i,d.score/15*125)[1]}" r="5" fill="${d.color}"/><text x="${point(i,160)[0]}" y="${point(i,160)[1]}" text-anchor="middle" dominant-baseline="middle">${d.name}</text>`).join('')}</svg>`;}
 function report(){if(!state.completed||!complete())return render();const rows=scores(),ranked=[...rows].sort((a,b)=>b.score-a.score),unique=[...new Set(rows.map(r=>r.score))],cut=ranked[2].score,top=unique.length===1?[]:ranked.filter(r=>r.score>=cut);return `${link}<section class="talent-report-heading"><span class="wonder-eyebrow">MY LITTLE CONSTELLATION</span><h1>这是此刻，<em>闪闪发光的你。</em></h1><p>八大天赋优势观察报告 · 24 / 24 题已完成</p><div class="talent-report-actions"><button class="button ghost" data-talent-action="review">回看 / 修改答案</button><button class="button ghost" data-action="export">导出探索记录</button></div></section><section class="talent-report-overview"><div>${radar(rows)}<p class="small-print">每维 3 题相加 · 原始分 3–15 · 非人群百分位</p></div><div><span class="talent-report-tag">DINGDONG 发现了</span><h2>${top.length?'这些方向，值得多给一点舞台。':'八种可能，此刻一样值得探索。'}</h2><div class="talent-top-tags">${(top.length?top:rows).map(d=>`<span style="--dim:${d.color}">${d.icon} ${d.name} <b>${d.score}/15</b></span>`).join('')}</div><p>${top.length?'这里展示本次得分前三位及同分维度。它们是你或家长目前更常观察到的表现，可以先从喜欢的一项开始。':'各维度得分相同，本次不强行排前三。先选一个好奇的活动，积累更多真实事例。'}</p><p>分数较低可能与接触机会、熟悉程度或当前状态有关。无需补齐所有“短板”，也不要用一次问卷限定未来。</p><a class="text-button" href="#explore">再看看我喜欢哪些兴趣岛 →</a></div></section><div class="talent-detail-heading"><h2>八个维度，每一个都被看见</h2><span>展开卡片，回看原题和答案</span></div><div class="talent-results-grid">${rows.map(d=>`<article class="talent-result-card" data-talent-dimension="${d.id}" style="--dim:${d.color}"><header><span class="talent-card-icon">${d.icon}</span><div><small>${d.nickname}</small><h3>${d.name}</h3></div><b class="talent-score">${d.score}<small> / 15</small></b></header><div class="talent-score-track"><i style="width:${d.score/15*100}%"></i></div><p>${d.desc}</p><p class="talent-score-reading">${d.score>=12?'本次回答中，这些表现较常被观察到。可以尝试更丰富的活动。':d.score>=8?'本次回答中，有些表现已被观察到。试着记录更具体的生活例子。':'本次回答中，这些表现暂时较少被观察到。可以先轻松体验，无需急着下结论。'}</p><div class="talent-activity"><b>这周试一次 · 10–15 分钟</b><p>${d.activity}</p><p>${d.steps}</p><small>和 DingDong 聊聊：${d.reflection}</small></div><p class="talent-career-note"><b>长大后可以了解</b><br>${d.careers}</p><details><summary>回看这个维度的 3 个回答</summary><ol>${questions.filter(q=>q.type===d.id).map(q=>`<li>${q.text}<b>${options[state.answers[q.id]-1]} · ${state.answers[q.id]} / 5</b></li>`).join('')}</ol></details></article>`).join('')}</div><div class="talent-method"><b>把报告用在陪伴里</b><p>本周先选一项：约定 10–15 分钟体验，留下一张作品或一句发现，过一周再聊“还想继续吗”。家长描述具体行为，如“你试了两种方法”，少用固定的天赋标签。</p><p>沿用原版八维观察问卷的题目、维度与计分；说明及行动建议已按观察语气整理。职业名称用于拓展认识，不是职业适配结论；相关职业仍有各自的学习与资质要求。</p></div>`;}
 function refresh(){bridge.render?.();}
 document.addEventListener('click',e=>{const b=e.target.closest('[data-talent-action]');if(!b||b.disabled)return;const action=b.dataset.talentAction;
  if(action==='start')question();
  if(action==='answer'){const v=Number(b.dataset.value);if(!valid(v))return;state.answers[questions[state.index].id]=v;state.completed=false;state.completedAt=null;save();refresh();question();document.querySelector(`[data-talent-action=answer][data-value="${v}"]`)?.focus();}
  if(action==='previous'&&state.index>0){state.index--;save();question();document.querySelector('#talent-question-title')?.focus();}
  if(action==='next'&&valid(state.answers[questions[state.index].id])){if(state.index<23){state.index++;save();question();document.querySelector('#talent-question-title')?.focus();}else if(complete()){state.completed=true;state.completedAt=new Date().toISOString();save();bridge.close?.();refresh();window.scrollTo({top:0});}}
  if(action==='review'){state.index=0;save();question();}
 });
 window.TalentExplorer={render,summary,scores,bind(b){bridge=b;},exportData(){return {...JSON.parse(JSON.stringify(state)),scores:state.completed?scores().map(({id,name,score})=>({id,name,score,max:15})):null};},reset(){state=blank();save();}};
})();
